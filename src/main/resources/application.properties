#Port Number Change
#server.port=9090


#context path default value : empty (starting path)
# server.servlet.context-path=/kiran


#DB properties
spring.datasource.url=jdbc:mysql://localhost:3306/bekkadit?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=manager


# JPA properties
spring.jpa.show-sql = true

# validate, update, none, create-drop, create 
# Update means its create table at once and data update at evrytime 
# Create means every time create new table in db and add data 
spring.jpa.hibernate.ddl-auto = update    

#Disable open session in view 
spring.jpa.open-in-view=false